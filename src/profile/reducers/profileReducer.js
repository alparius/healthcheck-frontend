
const initialErr = {
    loginError: false
}

const profileReducer = (state = initialErr, action) => {
    const newState = state;
    switch (action.type) {
        default:
            break;
    }
    return newState;
}

export default profileReducer